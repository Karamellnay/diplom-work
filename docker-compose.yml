
version: '3.7'

services:

  postgres:
    image: mysql:8.0.18
    ports:
      - '3306:3306'
    volumes:
      - .data:/var/lib/mysql
    environment:
      _ MYSQL_RANDOM_ROOT_PASSWORD=yes
      _ MYSQL_DATABASE=app
      _ MYSQL_USER=app
      _ MYSQL_PASSWORD=pass

